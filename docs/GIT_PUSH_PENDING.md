# Git pendiente de subir

El usuario indico que no puede subir a GitHub en este momento. Estos commits quedan locales y deben subirse despues.

## Plataforma `codesys-opcua-interface`

Ruta:

```powershell
cd C:\Users\jomes\Documents\Codex\2026-07-15\google-drive-plugin-google-drive-openai\github-projects\codesys-opcua-interface
```

Comando:

```powershell
git push origin main
```

Commit local pendiente conocido:

- `38a3aba Hito 7 Grafana and Node-RED integrations`

Si se agregan nuevos commits antes del push, el mismo comando subira toda la cola local.

## Gateway `column-gateway`

Ruta:

```powershell
cd C:\Users\jomes\Documents\Codex\2026-07-15\google-drive-plugin-google-drive-openai\github-projects\column-gateway
```

Comando:

```powershell
git push origin main
```

Nota: antes de subir, revisar `git status -sb` en ambos repositorios.
