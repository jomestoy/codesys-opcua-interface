# PLCopen XML

Este directorio queda reservado para exportaciones PLCopen XML importables en CODESYS.

No se incluye un binario `.project` porque no puede generarse ni validarse sin CODESYS Development System.
